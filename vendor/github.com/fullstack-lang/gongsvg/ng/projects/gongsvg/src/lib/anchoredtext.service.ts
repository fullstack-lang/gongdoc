// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { AnchoredTextDB } from './anchoredtext-db';

// insertion point for imports
import { LinkDB } from './link-db'

@Injectable({
  providedIn: 'root'
})
export class AnchoredTextService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  AnchoredTextServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private anchoredtextsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.anchoredtextsUrl = origin + '/api/github.com/fullstack-lang/gongsvg/go/v1/anchoredtexts';
  }

  /** GET anchoredtexts from the server */
  getAnchoredTexts(GONG__StackPath: string): Observable<AnchoredTextDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<AnchoredTextDB[]>(this.anchoredtextsUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched anchoredtexts')),
        catchError(this.handleError<AnchoredTextDB[]>('getAnchoredTexts', []))
      );
  }

  /** GET anchoredtext by id. Will 404 if id not found */
  getAnchoredText(id: number, GONG__StackPath: string): Observable<AnchoredTextDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.anchoredtextsUrl}/${id}`;
    return this.http.get<AnchoredTextDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched anchoredtext id=${id}`)),
      catchError(this.handleError<AnchoredTextDB>(`getAnchoredText id=${id}`))
    );
  }

  /** POST: add a new anchoredtext to the server */
  postAnchoredText(anchoredtextdb: AnchoredTextDB, GONG__StackPath: string): Observable<AnchoredTextDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    anchoredtextdb.Animates = []
    let _Link_TextAtArrowEnd_reverse = anchoredtextdb.Link_TextAtArrowEnd_reverse
    anchoredtextdb.Link_TextAtArrowEnd_reverse = new LinkDB
    let _Link_TextAtArrowStart_reverse = anchoredtextdb.Link_TextAtArrowStart_reverse
    anchoredtextdb.Link_TextAtArrowStart_reverse = new LinkDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<AnchoredTextDB>(this.anchoredtextsUrl, anchoredtextdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        anchoredtextdb.Link_TextAtArrowEnd_reverse = _Link_TextAtArrowEnd_reverse
        anchoredtextdb.Link_TextAtArrowStart_reverse = _Link_TextAtArrowStart_reverse
        // this.log(`posted anchoredtextdb id=${anchoredtextdb.ID}`)
      }),
      catchError(this.handleError<AnchoredTextDB>('postAnchoredText'))
    );
  }

  /** DELETE: delete the anchoredtextdb from the server */
  deleteAnchoredText(anchoredtextdb: AnchoredTextDB | number, GONG__StackPath: string): Observable<AnchoredTextDB> {
    const id = typeof anchoredtextdb === 'number' ? anchoredtextdb : anchoredtextdb.ID;
    const url = `${this.anchoredtextsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<AnchoredTextDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted anchoredtextdb id=${id}`)),
      catchError(this.handleError<AnchoredTextDB>('deleteAnchoredText'))
    );
  }

  /** PUT: update the anchoredtextdb on the server */
  updateAnchoredText(anchoredtextdb: AnchoredTextDB, GONG__StackPath: string): Observable<AnchoredTextDB> {
    const id = typeof anchoredtextdb === 'number' ? anchoredtextdb : anchoredtextdb.ID;
    const url = `${this.anchoredtextsUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    anchoredtextdb.Animates = []
    let _Link_TextAtArrowEnd_reverse = anchoredtextdb.Link_TextAtArrowEnd_reverse
    anchoredtextdb.Link_TextAtArrowEnd_reverse = new LinkDB
    let _Link_TextAtArrowStart_reverse = anchoredtextdb.Link_TextAtArrowStart_reverse
    anchoredtextdb.Link_TextAtArrowStart_reverse = new LinkDB

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<AnchoredTextDB>(url, anchoredtextdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        anchoredtextdb.Link_TextAtArrowEnd_reverse = _Link_TextAtArrowEnd_reverse
        anchoredtextdb.Link_TextAtArrowStart_reverse = _Link_TextAtArrowStart_reverse
        // this.log(`updated anchoredtextdb id=${anchoredtextdb.ID}`)
      }),
      catchError(this.handleError<AnchoredTextDB>('updateAnchoredText'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in AnchoredTextService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("AnchoredTextService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
